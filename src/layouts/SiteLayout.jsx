import React from 'react';
import Header from '../components/Headers';

const SiteLayout = () => {
    return (
        <>
            <Header />
        </>
    );
};

export default SiteLayout;