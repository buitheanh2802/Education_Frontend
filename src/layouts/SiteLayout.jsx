import React from 'react';
import Header from '../components/Header';

const SiteLayout = () => {
    return (
        <>
        <Header/>
        </>
    );
};

export default SiteLayout;